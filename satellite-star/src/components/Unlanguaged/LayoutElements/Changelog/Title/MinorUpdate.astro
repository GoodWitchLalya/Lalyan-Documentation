---
import Update from "../Title/Update.astro";

interface Props {
    version: string;
}

const { version } = Astro.props;
const color = "#3598DB";
---

<Update version={version} color={color}>
<slot />
</Update>
